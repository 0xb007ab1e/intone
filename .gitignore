# Rust
/target/
**/*.rs.bk
*.pdb
# Cargo.lock IS committed — this workspace builds binaries (reproducible builds).

# Local dev / secrets (never commit) — see ruleset workflow-secrets
.env
.env.*
!.env.example

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
